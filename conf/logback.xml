<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/card-payment-frontend.log</file>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%date{ISO8601} level=[%level] logger=[%logger] thread=[%thread] message=[%message] %replace(exception=[%xException]){'^exception=\[\]$',''}%n</pattern>
        </encoder>
    </appender>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%green(%date{ISO8601}) %cyan(level=)[%highlight(%level)] %cyan(message=)[%yellow(%message)] %cyan(logger=)[%white(%logger)] %cyan(sessionId=)[%white(%X{X-Session-ID})] %gray(thread=)[%gray(%thread)] %gray(rid=)[%gray(%X{X-Request-ID})] %gray(user=)[%gray(%X{Authorization})] %replace(exception=[%xException]){'^exception=\[\]$',''}%n</pattern>
        </encoder>
    </appender>

    <appender name="CONNECTOR_LOG_FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/connector.log</file>
        <encoder>
            <pattern>%message%n</pattern>
        </encoder>
    </appender>


    <logger name="com.google.inject" level="INFO"/>

    <logger name="uk.gov" level="WARN"/>

    <logger name="application" level="DEBUG"/>

    <logger name="kibana-logger" level="INFO"/>

    <logger name="connector" level="TRACE">
        <appender-ref ref="STDOUT"/>
    </logger>

    <root level="INFO">
        <appender-ref ref="FILE"/>
        <appender-ref ref="STDOUT"/>
    </root>
</configuration>
